# Copy this file to .env and fill in your values.

# Google Apps Script Web App deployment URL (see google-apps-script/Code.gs)
VITE_SHEETS_API_URL=

# Password to access the admin login (change this before going live!)
VITE_ADMIN_PASSWORD=atharvluxe2026

# Google OAuth Client ID for "Continue with Google" login.
# Create one at https://console.cloud.google.com/apis/credentials
# (Create Credentials > OAuth client ID > Web application), and add
# your site's domain under "Authorized JavaScript origins".
VITE_GOOGLE_CLIENT_ID=
